[build]
dockerfilePath = "backend/Dockerfile"
buildContext = "backend"

[deploy]
startCommand = "sh scripts/start/app.sh"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
